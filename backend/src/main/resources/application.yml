spring:
  application:
    name: diagrammnt
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 10000
  data:
    redis:
      lettuce:
        pool:
          max-active: 16
          max-idle: 8
          min-idle: 4
          max-wait: 5000ms
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB
      enabled: true
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
    default-property-inclusion: non_null
server:
  port: ${APP_PORT:8080}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
    map-underscore-to-camel-case: true
    cache-enabled: false
  global-config:
    db-config:
      id-type: auto
  mapper-locations: classpath*:/mapper/**/*.xml
logging:
  config: classpath:logback-spring.xml
  level:
    root: ${LOG_LEVEL:INFO}
    com.diagrammnt: ${LOG_LEVEL:INFO}
    com.baomidou.mybatisplus: WARN
    org.springframework.data.redis: WARN
sso:
  jwt:
    secret: 8s9D7f8G6d5S4a3Q2w1E9r8T7y6U5i4O3p2A1s0D9f8G7h6J5k4L3j2B1v0C9x8S7a6D5f4G3h2J1
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized
